// Entry point for route with imported components for the Get Involved page

import React from 'react'
import './GetInvolved.css'

function GetInvovled() {
/*
    const [ name, setName ] = useState("")
    const [ email, setEmail ] = useState("")
    const [ password, setPassword ] = useState("")*/
    
  return (
    /*<form action="">
        <input onChange ={e => setName(e.target.value)} value={name} type="text" placeholder="Name" />
        <input onChange ={e => setEmail(e.target.value)} value={email} type="email" placeholder="Email" />
        <input onChange ={e => setPassword(e.target.value)} value={password} type="password" name="pwd" id="pwd" placeholder="Password" />
        <button type="button">Submit</button>
    </form>*/
  )
}

export default GetInvovled