// directory of images to easily import and use dynamically

// * actual images are stored in the Images folder then imported here. 
// * images are added to the Images folder in three ways: drag and drop from finder/file explorer to VSCode, opening your finder/file explorer and moving images in the files there (should then be in appropriate files in VSCode), or download image from web directly into the file you want when you are prompted to do so